export enum Environment {
  Development = 'dev',
  Production = 'prod',
  Test = 'test',
  Debug = 'debug'
}

export enum LogLevel {
  Error = 'error',
  Warn = 'warn',
  Log = 'log',
  Verbose = 'verbose',
  Debug = 'debug'
}

export enum PrismaLogLevel {
  Query = 'query',
  Info = 'info',
  Warn = 'warn',
  Error = 'error',
}
